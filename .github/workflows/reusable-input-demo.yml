name: Resusable input Demo
on:  
  workflow_call:
    inputs:
      name:
          description: Enter the name
          required: true
          type: string

jobs:
   greeting:
     runs-on: ubuntu-latest
     steps:
       - name: Greeting Step
         run: echo "hi ${{ inputs.name }} Welcome to Resubale Workflow "
